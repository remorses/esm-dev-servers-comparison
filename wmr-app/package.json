{
    "name": "wmr-app",
    "version": "0.0.0",
    "scripts": {
        "dev": "wmr",
        "build": "wmr build --prerender",
        "serve": "wmr serve"
    },
    "eslintConfig": {
        "extends": "preact"
    },
    "alias": {
        "react": "preact/compat",
        "react-dom": "preact/compat"
    },
    "dependencies": {
        "preact-iso": "^0.2.0",
        "common": "*"
    },
    "devDependencies": {
        "wmr": "^1.0.0"
    }
}
